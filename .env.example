# Supabase (safe to expose client-side; access is enforced by Row Level Security)
VITE_SUPABASE_URL=https://amqkhmptfnizuhtphucq.supabase.co
VITE_SUPABASE_ANON_KEY=replace-with-anon-key

# Server-side only (used by /api functions). Leave unset to use the built-in mock/stub providers.
VALUATION_PROVIDER=mock
AI_TIPS_PROVIDER=stub
